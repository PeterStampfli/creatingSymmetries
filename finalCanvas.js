"use strict";
// the final canvas, repeating the output canvas

var finalCanvas=new PixelCanvas("finalCanvas");

	  	finalCanvas.setSize(2*initialOutputWidth,2*initialOutputHeight);
	  	finalCanvas.blueScreen();